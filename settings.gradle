pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        //引用通过JitPack.io发布的第三方库
        maven { url 'https://jitpack.io' }
    }
}


rootProject.name = "ArrayShow"
include ':app'
